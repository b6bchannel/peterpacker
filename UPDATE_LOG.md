# Packer Update Log

## 2026-07-29｜手机 PWA 启动速度第一阶段优化

### 本次更新

- 增加版本化的离线应用缓存。
- 第一次联网打开后，首页脚本、样式、字体、图标和 PWA 配置会保存在手机本地。
- 之后从手机主屏幕打开时优先使用手机本地文件，不再等待 GitHub Pages 返回首屏文件。
- 增加离线启动能力；网络不可用时仍能打开已经缓存过的 Packer。
- 增加轻量启动界面。在 React 和用户数据准备完成前，先显示 Packer 标题和清单骨架，避免长时间空白。
- 增加 iPhone 主屏幕 Web App 相关配置。
- 取消字体的首屏强制预加载，让脚本和页面样式优先。
- 离线缓存使用明确文件清单，不会扫描或下载整个 `assets` 文件夹。
- 清理本地 `assets` 中 36 个未被当前页面引用的旧脚本、旧样式和旧图片。
- 项目文件总量从约 6.8 MB 降至约 0.5 MB。
- 新增 `GITHUB_UPLOAD_GUIDE.md`，说明非技术用户如何更新 GitHub Pages。

### GitHub Pages 检查结果

- 线上脚本已启用 Gzip 压缩。
- GitHub Pages 的普通浏览器缓存时间为 10 分钟。
- 测试环境中，首页和首屏脚本的单次请求约需要 1.1 秒，因此手机冷启动容易受到网络影响。
- 新增的离线应用缓存会在第一次联网打开后避开这段等待。

### 已完成检查

- 主应用脚本语法检查通过。
- Service Worker 脚本语法检查通过。
- 当前页面引用的全部文件存在。
- 首次正常启动通过。
- 第二次缓存启动通过。
- 关闭测试服务器后，离线启动通过。
- 测试中的离线启动约为 52 毫秒。
- 离线启动后页面内容和本机用户数据正常显示。
- 浏览器控制台没有发现错误或警告。

### 发布注意事项

- 本次更新不需要删除或重建 GitHub 仓库。
- 第一次升级后需要联网打开一次，让手机安装离线缓存。
- 以后每次发布新版本时，需要同步修改 `sw.js` 的缓存版本名称。

## 2026-07-29｜备份格式第二轮升级（版本 3）

### 本次更新

- 新导出的备份升级为格式版本 3。
- 备份不再重复保存 Packer 自带的整套默认物品、默认模块和默认箱包，只保存用户自己的内容和对默认内容做过的改动。
- 旅行与旅行中的打包状态仍然完整保存，恢复后会保持备份时的勾选状态。
- 用户新增的物品、分类、模块和箱包会完整保存。
- 用户修改过的默认物品、默认模块和默认箱包只保存修改的部分。
- 用户主动删除的默认物品会单独记录，避免恢复后被错误补回。
- 打包规则只保存用户实际改过的规则，不再把整套默认规则写进备份。
- 为默认物品和默认箱包增加稳定识别标记；即使以后显示名称发生变化，也能更可靠地找到同一项内容。
- 恢复版本 3 备份时，先使用当前 Packer 版本的默认内容打底，再叠加备份中的个人内容和改动。
- 在有本机个人数据时，“保留当前内容并恢复”继续优先保留本机物品库内容；同一旅行仍直接使用备份中的版本。
- 设备没有个人数据时，会直接完整采用备份里的个人改动，同时自动使用当前版本的默认内容。

### 旧备份兼容

- 格式版本 1 和版本 2 的备份仍然可以恢复。
- 读取旧备份时，不会因为旧文件里缺少后来新增的默认物品，就把这些新版默认物品当作已删除。
- 旧备份中的整套规则会与当前默认规则比较，只把确实不同的部分当作用户改动。
- 来自未来更高格式版本的备份仍会被阻止，并提示先更新 Packer。

### 已完成检查

- 页面脚本语法检查通过。
- 实际导出的文件确认为格式版本 3。
- 导出文件包含旅行、打包状态、自定义物品、自定义模块、自定义箱包和用户规则改动。
- 导出文件不包含整套默认物品列表。
- 格式版本 3 的备份预览、恢复和恢复结果检查通过。
- 格式版本 2 的旧备份预览与安全恢复通过。
- 更高格式版本的备份拦截提示通过。
- 恢复后新版默认物品仍然保留。
- 浏览器控制台没有发现运行错误。

## 2026-07-29｜备份与恢复体验升级

### 本次更新

- 将“导入”改为更容易理解的“恢复备份”。
- 使用“保留当前内容并恢复”和“让这台设备回到备份时”描述恢复结果，不再使用“合并导入”和“覆盖现有数据”。
- 选择备份后先显示创建时间、旅行数量、物品数量、已打包数量和恢复后的具体影响。
- 当前设备没有旅行时，直接使用安全恢复，不要求用户理解恢复方式。
- 同一旅行在当前设备和备份中同时存在时，直接使用备份中的版本，不再生成带括号的副本。
- 安全恢复会保留当前设备独有的旅行，并补充备份中缺少的旅行和物品。
- 安全恢复默认保留当前打包规则；用户可以主动选择同时恢复备份中的规则。
- 恢复备份后会重新补齐当前版本的默认物品、分类和箱包，避免老备份移除新版默认内容。
- “让这台设备回到备份时”会恢复备份中的旅行、物品库和规则，同时仍会补齐新版默认内容。
- 每次恢复前自动保存一份本机恢复点，并提供“撤销上次导入”。
- 撤销前增加二次确认，明确提示导入后的修改也会被撤销。
- 增加备份文件检查：错误 JSON、非 Packer 文件、数据不完整和来自更高版本的备份会被阻止。
- 新导出的备份升级为格式版本 2，并继续兼容原有格式版本 1 的备份。
- 重新设计恢复界面，减少胶囊按钮，使用清晰的分区、直角列表和少量矩形操作按钮，避免胶囊套胶囊。

### 数据兼容性

- 原有版本 1 备份可以继续恢复。
- 新版备份增加 `format`、`appVersion` 和格式版本信息，方便以后升级时检查兼容性。
- 安全恢复时，备份中缺少的新版规则不会覆盖当前规则。
- 同一旅行发生冲突时，以备份内容为准，包括旅行名称、物品和打包状态。

### 已完成检查

- 页面脚本语法检查通过。
- 空设备恢复旧版备份通过。
- 同一旅行冲突时直接采用备份版本通过。
- 当前规则默认不被老备份覆盖通过。
- 用户主动恢复备份规则通过。
- 导入前恢复点和撤销操作通过。
- 无效备份文件提示通过。
- 桌面宽度和手机宽度的恢复界面检查通过。
- 浏览器控制台没有发现错误或警告。
